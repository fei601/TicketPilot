# ===========================================
# TicketPilot 依赖
# Python 3.10+
# ===========================================
# 口径：只列代码实际 import 的库（2026-09-22 审计）。
# 此前列表含 langchain/chromadb/sentence-transformers——代码从未引用，
# 且与 README「无向量库」表述自相矛盾；部署时还会拖慢构建数分钟。

# LLM 调用（OpenAI 兼容接口）
openai>=1.0.0

# 后端 API
fastapi>=0.100.0
uvicorn>=0.23.0

# 前端
streamlit>=1.28.0

# 数据模型与校验
pydantic>=2.0.0

# HTTP 请求（Agent 域演出查询/降级搜索）
requests>=2.31.0

# 环境变量
python-dotenv>=1.0.0

# 遗留模块依赖（播报/调度等早期功能，代码保留在 repo）
apscheduler>=3.10.0
ntplib>=0.4.0

# 测试
pytest>=7.4.0
